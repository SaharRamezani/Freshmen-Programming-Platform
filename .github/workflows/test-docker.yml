name: Run Selenium Tests with Docker

on:
  push:
    branches: [ main, develop, sp1_test_github_actions ]
  pull_request:
    branches: [ main, develop, sp1_test_github_actions ]
  workflow_dispatch:

jobs:
  test-docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Install Chrome
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable

      - name: Install ChromeDriver
        uses: nanasess/setup-chromedriver@v2

      - name: Clean up any existing containers
        run: |
          docker compose down -v || true
          docker system prune -f || true

      - name: Create .env file from secrets
        run: |
          echo "JWT_SECRET_KEY=${{ secrets.JWT_SECRET_KEY || 'test-jwt-secret-key-for-ci-testing-only' }}" > .env
          echo "SECRET_KEY=${{ secrets.SECRET_KEY || 'test-secret-key-for-ci-testing-only' }}" >> .env
          echo "GOOGLE_OAUTH_CLIENT_ID=${{ secrets.GOOGLE_OAUTH_CLIENT_ID || 'test-client-id' }}" >> .env
          echo "GOOGLE_OAUTH_CLIENT_SECRET=${{ secrets.GOOGLE_OAUTH_CLIENT_SECRET || 'test-client-secret' }}" >> .env
          echo "GOOGLE_OAUTH_REDIRECT_URI=${{ secrets.GOOGLE_OAUTH_REDIRECT_URI || 'http://localhost:8000/auth/callback' }}" >> .env

      - name: Start services with Docker Compose
        run: |
          docker compose build --no-cache
          docker compose up -d

      - name: Wait for services to be healthy
        run: |
          echo "Waiting for API..."
          timeout 60 bash -c 'until curl -f http://localhost:8000/docs > /dev/null 2>&1; do sleep 2; done'
          echo "API is ready"
          
          echo "Waiting for frontend..."
          timeout 60 bash -c 'until curl -f http://localhost:3000 > /dev/null 2>&1; do sleep 2; done'
          echo "Frontend is responding"
          
          sleep 5
          echo "Services ready"

      - name: Show service logs
        if: always()
        run: docker compose logs

      - name: Run Selenium tests
        run: |
          cd test
          mvn clean test -Dheadless=true
        env:
          CI: true

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: test/target/surefire-reports/
          retention-days: 7

      - name: Stop services
        if: always()
        run: docker compose down

      - name: Clean up .env file
        if: always()
        run: rm -f .env
